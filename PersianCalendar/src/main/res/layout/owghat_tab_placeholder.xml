<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/owghat_content"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackgroundBorderless"
    android:orientation="vertical"
    android:padding="@dimen/padding_item">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="@dimen/padding_item"
        android:text="برای دیدن اوقات شرعی مکان را در تنظیمات مشخص کنید." />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <Button
            android:id="@+id/discard"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/padding_item"
            android:layout_weight="1"
            android:text="مخفی‌سازی برگه"
            android:textColor="?attr/colorSecondaryVariant" />

        <Button
            android:id="@+id/activate"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="@dimen/padding_item"
            android:layout_weight="1"
            android:text="رفتن به تنظیمات"
            android:textColor="?attr/colorPrimary" />

    </LinearLayout>

</LinearLayout>

